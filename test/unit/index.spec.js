/* global describe, it, expect */
describe('node-tsheets-client Library', () => {
  describe('constructor', () => {
    it('has unit test scaffolding', () => {
      expect(1).to.equal(1)
    })
  })
})
