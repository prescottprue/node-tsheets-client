{
  "name": "tsheets-client",
  "version": "1.0.5",
  "description": "Client to the TSheets REST API, for time reporting etc.",
  "main": "tsheets-client.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "NODE_ENV=test istanbul cover --report teamcity --report lcov _mocha -- --ui bdd --check-leaks --recursive --slow 500 --timeout 5000 --reporter spec --globals 'Intl,IntlPolyfill' test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Springworks/node-tsheets-client.git"
  },
  "keywords": [
    "tsheets",
    "time",
    "tracking"
  ],
  "author": "Springworks",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Springworks/node-tsheets-client/issues"
  },
  "homepage": "https://github.com/Springworks/node-tsheets-client",
  "devDependencies": {
    "chai": "^1.10.0",
    "eslint": "^0.11.0",
    "istanbul": "^0.3.5",
    "mocha": "^2.1.0"
  },
  "dependencies": {
    "joi": "^5.1.0",
    "request": "^2.51.0"
  }
}
