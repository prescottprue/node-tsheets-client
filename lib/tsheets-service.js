'use strict';

var api_service = require('./api-service.js');


exports.reportTime = reportTime;


function authenticateUser() {
  
}


function reportTime(params, callback) {
  // TODO: validate params
  // var params = { job_code_id: '', user_id: '', type: 'manual', duration: seconds, date: date } ;
  // api_service.makeRequest(token, '/timesheets', params, function(err, result) {});
}
